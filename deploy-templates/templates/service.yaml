apiVersion: v1
kind: Service
metadata:
  name: {{ include "dotnet-6-0-clone-hub-semver-build.fullname" . }}
  labels:
    {{- include "dotnet-6-0-clone-hub-semver-build.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dotnet-6-0-clone-hub-semver-build.selectorLabels" . | nindent 4 }}
